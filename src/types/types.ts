export type thoughts = {
    handle : string
    title : string
    text : string
    id: number
    date : string
    ra : boolean
};

export type songs = {
    songs_href : string
    songs_name : string
};

export type poems = {
    poems_title : string
    poems_text : string
};