export type thoughts = {
    text : string
    id: number
    date : string
};

export type songs = {
    songs_href : string
    songs_name : string
};

export type poems = {
    poems_title : string
    poems_text : string
};